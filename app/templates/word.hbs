{{search-box word=model.word submit=(route-action 'searchWord')}}

{{#each-in groupedDefinitions as |partOfSpeech definitions|}}
  {{definition-card definitions=definitions}}
{{else}}
  {{#md-card title='Error' class='error'}}
    {{#md-card-content}}
      Word not found.
    {{/md-card-content}}
  {{/md-card}}
{{/each-in}}
